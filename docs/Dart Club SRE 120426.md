### Major Technical Issues (High Priority)
- Add Turkish language toggle button
- Let the login forms on login page to be expanded by default if resolution is small.(Form expansion relies on hovering; how can a mobile user hover on the unexpanded form?)
- Force strong password selection, but not too strong(I literally signed up with 1234).
- the up arrow button(bottom right of the page) is overlapping with social links.
- Nice job with the Tournament Bracket. LOOKS COOL! But it looks pathetic on mobile. Both focus mode and default are messed up on smaller devices.


### Minor Technical Issues (Moderate Priority)
- On Gallery, add a close ("X") button for closing an image when viewing.(Not every uesr may know clicking outside an image closes it)
- When user is already on focus mode, button text should change to "close focus mode".

### Missing Content (Moderate Priority)
- Add about dev page with our images linked to our linkedin page. Add a an image placeholder for Nazife Dimililer as well as project manager.
- Can transition to other pages be animated/improved visually so website looks more modern/smooth?
- Add Neo-DoubleEdged logo. Also add my personal logo(I'll send you later) and your own if you have one to the logos section.

### Visuals and Aesthetics (Low Priority)
- on "about" section push the dart image to right a bit.(it should be a bit further from the center of the page)
- Make both red buttons(on main page, tournaments page, and register page) and other minor buttons(like the ones on blog page) glow "harder"/"brighter" on hover. Text color can also be changed on hover.
- Navbar and login page are perfect(shout out to me of course), but on login page make the sign in button more red and less orange. Also turn "Sign up " to a button(visuals) not just text; someone might assume it's just text.
- Add light/dark mode.(super-optional and unnecessary )
- For fixture list(and every other card below) clicking on the "fixture-selection-toggle" expands/reduces the card. The number of items there should be a static number and there should be another tiny (arrow-like) button for expanding/minimizing.


### Other things to add (Completely optional)
- An email service to send you an email once you signed up.(Same can be used to verify)



## TODO:
- Test the tournament creation functionalities and try to break it.