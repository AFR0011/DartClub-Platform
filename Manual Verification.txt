1. Blog page needs a UI/UX redo, it doesn't flow properly, it's not clear where a post starts and where another ends, can only add one image and it's too big when displayed, the text areas action buttons are not clearly separated from the post itself. I was thinking of making it like news sites where a preview of blogs is shown in the main page, and you can click to see the full post, and maybe have some navigation arrows or buttons to go to next/previous post when we're actually on a post. And for creating posts add a button that brings up the current "create a blog post section" in a different page or as a dialogue, whichever makes more sense UI/UX-wise.


2. When adding images through the blog posts, the gallery image display is overwritten. Let's keep the same style of "4 images per row with titles underneath" display that the placeholder images "Gal1" - "Gal6" currently use, and let's make it so that when you open an image that is linked to a blog post, it will let you go to that post.

3. When making changes like saving the scores of a match, or changing the match that a player belongs to, or anything else that changes the DB, the tournament page refreshes completely to fetch the results and it goes back automatically to the matches view and resets. When doing operations, it should be seamless, no resetting.

4. Tournament bracket matchups are still overlapping a bit, I increased ~15% --bracket-track of .connected-bracket and it looks good now, maybe that helps with adjustments.

5. Add color coding to things so it's easier to understand what's happening in the tournament matchups (e.g., make the background color of matches green/yellow/white based on completed/in progress/scheduled, change the color of the winner or highlight them somehow, etc.). Come up with a list of these changes.

6. Do a full pass over the website and suggest additions/modifications, whether its UI/UX or features.