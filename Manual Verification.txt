1. Make bracket view a bit smaller, right now the match entries are too big and a lot of things are overlapping. Make it so that the cards (current style) show up only when a match up is clicked.

2. Also do a UI pass on the blog and gallery pages, especially their buttons and text inputs, they are just base HTML elements it seems like.

